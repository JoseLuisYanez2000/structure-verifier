/**
 * Configuracion global del verificador.
 * Define el idioma por defecto utilizado por todos los mensajes de error.
 */
export class VerifierConfig {
    /**
     * Idioma actual utilizado para los mensajes de verificacion.
     * Puede ser 'es' (espanol) o 'en' (ingles). Valor por defecto: 'es'.
     */
    static lang: 'en' | 'es' = 'es';
}
